version: '3'

services:
        cookiecutter:
                build:
                        context: .
                        args: 
                                repo: ${TEMPLATE_REPO_URL}
                                release: ${REPO_RELEASE_VERSION}
                                python_image_verson: ${PYTHON_IMAGE_VERSON}
                image: ${DOCKER_USERNAME}/cookiecutter:initiator
                container_name: flask-mysql-ci-initiator
                command: tail -f /dev/null
